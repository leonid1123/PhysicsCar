using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class varGiver : MonoBehaviour
{
    // Start is called before the first frame update
    public int DDD;
    void Start()
    {
        DDD = 10;
    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
